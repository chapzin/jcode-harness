{
  "id": "gate-2026-05-08-b56976",
  "ts": "2026-05-08T18:00:45.559Z",
  "contractId": "2026-05-08-defer-busy-modelsupdated-pushes-for-remote-clients-18eebf",
  "verdict": "pass",
  "outputChecks": [
    {
      "path": "src/server/client_lifecycle.rs",
      "exists": true
    },
    {
      "path": "src/server/provider_control_tests.rs",
      "exists": true
    }
  ],
  "checkedConditions": [
    "ModelsUpdated bus events are not permanently lost when the session agent lock is busy; busy try_lock path now returns Busy and leaves pending_models_updated set.",
    "Busy lock path marks a pending model update and retry/final processing boundary flushes AvailableModelsUpdated if the encoded snapshot changed.",
    "Focused test models_updated_push_defers_when_agent_lock_is_busy covers busy lock, later successful send, and duplicate snapshot suppression.",
    "Focused verification, cargo fmt/check, cargo check, git diff --check, selfdev build and reload all passed."
  ],
  "uncheckedConditions": [
    "ModelsUpdated bus events are not permanently lost when the session agent lock is busy.",
    "Busy lock path marks a pending model update and a later idle/request boundary flushes AvailableModelsUpdated if the snapshot changed.",
    "Focused tests cover busy lock skip followed by successful deferred AvailableModelsUpdated send.",
    "Focused tests, cargo fmt/check, cargo check, selfdev build/reload, governance, commit/push, and issue #27 update pass."
  ],
  "evidence": [
    "/tmp/jcode-bg-tasks/046856pwmp.output",
    "/tmp/jcode-bg-tasks/147954p86u.output",
    "src/server/client_lifecycle.rs",
    "src/server/client_lifecycle_tests.rs"
  ],
  "notes": "Retry uses a short 250ms interval gated by client_subscribed && pending_models_updated, plus an immediate flush attempt after processing completion. Snapshot encoding still suppresses duplicate AvailableModelsUpdated frames and preserves oversized-frame guard behavior."
}
